# 2026-04-27

## 新网站重建 - Umami Sales POS (浅色主题)

### 完成的工作
- 创建新的静态网站，浅色主题（#f8fafc 背景）
- 样式保持与旧网站完全一致的布局和结构
- 去除 IndexedDB，数据仅通过 localStorage 暂存 + Google Sheets 同步
- 保留 Excel 导出功能

### 文件结构
```
Umami Sales_APP20260727/
├── split.html      # 主入口，分屏视图
├── order.html      # 订单页面
├── kitchen.html    # 厨房显示
├── app.js         # 核心逻辑 (localStorage + Google Sheets)
├── styles.css     # 浅色主题样式
├── manifest.json  # PWA 配置
├── sw.js          # Service Worker
└── .workbuddy/    # 记忆文件
```

### 待完成
1. **图标文件**：需要 icon-192.png 和 icon-512.png（可从旧网站复制）
2. **Google Sheets Apps Script**：需要创建并部署，配置 Web App URL
3. **部署到 Vercel**：上传所有文件到新 Vercel 项目

### 已完成的改进
- **History 加载动画**：点击 History 按钮时显示 "Syncing with Google Sheets..." 旋转加载状态
- **Kitchen 卡片动画**：单击/双击卡片时显示同步动画（蓝色/绿色边框闪烁 + 旋转图标）

### Google Sheets 数据结构
- **localStorage**: 订单和产品数据本地暂存
- **同步机制**: 通过 Google Apps Script Web App POST 数据
- **同步方式**: no-cors（单向写入，无法读取响应）

### 产品默认配置
15 个产品（与旧网站一致）：
- 🍣 Sushi: Pieni(€11.5), Pieni+(€14), Medium(€16.5), Iso(€19.5)
- 🥤 Drinks: Drink(€2), Nigri(€1.8)
- ➕ Addon: *Take away(€0)
- 💰 Discounts: -Student, -Vege, -Vegan, -All Fry, -All Raw, -No Mayo, -No Dessert, -No Tofu(GF)
